/*
 * Para armazenar o tipo de cutscene que pode ser apresentada.
*/
public enum CutsceneType
{
    EXTERNAL,       // se a cutscene é externa (.mp4)
    INSIDE_EDITOR   // se o cutscene é criada dentro do unity com a funcionalidade Timeline
}